---
id: redundant-boolean-return
language: python
severity: error
message: Replace constant boolean branches with an explicit boolean expression.
rule:
  any:
    - pattern: |
        if $COND:
          return True
        else:
          return False
    - pattern: |
        if $COND:
          return False
        else:
          return True
